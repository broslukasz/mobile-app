import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.pgs.mobile',
  appName: 'MobileApp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
